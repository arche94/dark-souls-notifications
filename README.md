# Dark Souls notifications

Dark Souls You Died styled notification system for browsers.

## Project Structure

- **Source files**: `src/**/*.ts` (TypeScript)
- **Compiled files**: `src/**/*.js` (generated by TypeScript compiler)
- **Type definitions**: `@types/chrome` for Chrome extension APIs

## Supported Browsers

- Microsoft Edge (143.0.3650.96+)
- Google Chrome (143.0.7499.170+)
- Opera Browser (125.0.5729.49+)

## Building the Extension

### Install Dependencies

```bash
npm install
```

### Build TypeScript

```bash
npm run build
```

### Watch Mode (Development)

```bash
npm run watch
```

### Clean Build Files

```bash
npm run clean
```

Removes all compiled `.js` files (and `.js.map` source maps).

## Development Workflow

1. Start dynamic recompiling with `npm run watch`
2. Edit TypeScript files in `src/**/*.ts`
3. Reload the extension in your browser
4. Relead the browser pages where you're testing the extension
5. Test your changes
